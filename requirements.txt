# Core
pyyaml>=6.0
httpx>=0.27
anthropic>=0.40           # Claude decision layer
tenacity>=8.2             # retry/backoff on RPC 429

# Optional sponsor integrations
# bnbagent                # ERC-8004 on-chain identity (BSC testnet) — install when wiring SDK

# Dev
pytest>=8.0
